[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "mta-data-analytics"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]

[tool.basedpyright]
typeCheckingMode = "standard"
